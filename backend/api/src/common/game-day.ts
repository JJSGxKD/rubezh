/**
 * Граница игровых суток — полночь по Москве, одна на все суточные механики:
 * потолки кошелька, задания, награда дня, колесо, дневные рейтинги, возвраты
 * воронки (`05-game-design.md` §3). Две разные границы — это задание, которое
 * обновилось, а награда дня ещё нет.
 *
 * Сутки считает база (`AT TIME ZONE`), а не код: так граница одна и та же у
 * всех реплик и не зависит от часового пояса процесса.
 */
export const GAME_DAY_TIME_ZONE = "Europe/Moscow";
