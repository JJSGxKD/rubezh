/**
 * Числа рассылок (docs/29-admin-panel.md §7, §8). Меняются здесь, в карте
 * конфигурации — docs/30-configuration-map.md.
 */
export const BROADCAST_RULES = {
  /**
   * Аудитория больше этого — правило двух ключей: запускает один, одобряет
   * другой (§8). Меньшую — тест на своих, служебную — запускает один человек.
   */
  approvalAudience: 1000,
  /** по умолчанию не писать тем, кто получал рассылку за столько дней — лимит частоты на игрока */
  skipRecentDays: 3,
  /** столько раз площадка может попросить подождать одного игрока, потом — отказ */
  maxDeferrals: 5,
  /** пачка очереди — столько секунд отправки в темпе площадки */
  batchSec: 4,
  /** на сколько задание очереди берёт строки: упавшее отпустит их по сроку */
  claimSec: 60,
} as const;
