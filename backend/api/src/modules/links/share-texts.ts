/**
 * Тексты шеринга (docs/24-attribution-and-sharing.md §7.5). Пока — константы
 * здесь; варианты для A/B и правка из панели придут с конфигурацией контента
 * (`19-content-admin.md`). Род игрока неизвестен — формулировки без него.
 */
export const SHARE_TEXTS = {
  run: {
    /** подпись к ссылке, которую игрок отправит сам */
    message: (time: string) => `Мой рекорд на «Рубеже» — ${time}. Сможешь дольше?`,
    /** превью ссылки в мессенджере */
    ogTitle: (name: string, time: string) => `${name}: ${time} на «Рубеже»`,
    ogDescription: "Продержись под натиском как можно дольше — прямо в Telegram.",
  },
} as const;
