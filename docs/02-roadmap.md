# Роадмап

Путь до софт-лонча **в Telegram** — пять этапов, дальше публичный запуск,
порт на MAX, затем VK. Переиспользование существующего бэкенд-стека и
готовых наработок из `vpnsibcom_api` / `vpnsibcom_web` (см.
`13-reuse-from-vpnsibcom.md`).

## Этапы вместо недель

Изначально роадмап был разбит по календарным неделям. Первая «неделя» заняла
2–3 дня: команда работает с ИИ-агентами, и календарная сетка перестала
описывать реальность — она одновременно завышала сроки одних задач и
скрывала, что другие от агентов не ускоряются вовсе.

Поэтому единица планирования — **этап**: законченный результат с критерием
готовности. Рядом с каждым этапом — **оценка** в рабочих днях, которая
пересматривается на старте этапа и по его итогам записывается фактом.

Три правила оценок:

- **Оценка — не обещание.** Она нужна, чтобы увидеть, что этап разросся, а
  не чтобы в него уложиться ценой критерия готовности.
- **Агент ускоряет не всё.** Код и документация ускоряются кратно;
  визуальные решения, подбор ассетов, ручное QA на устройствах, модерация и
  верификация на площадках — нет.
- **Часть этапов привязана к календарю.** Retention D7 не измерить быстрее,
  чем за семь дней от когорты, верификацию партнёра на MAX не ускорить
  (`03-notes-and-risks.md`). Такие этапы оцениваются в календарных днях и
  помечены.

**Соответствие старой нумерации — один к одному**: «неделя N» в документах,
написанных до перехода, означает «этап N»; «недели 7–8» — этап 7. Сквозная
замена в остальных документах — отдельной задачей (`26-stage2-plan.md`,
WP12).

| Этап | Результат | Оценка |
|---|---|---|
| 1 | Технический прототип, решение go/no-go по движку | **факт: 2–3 дня** (план — неделя) |
| 2 | Игровой цикл, оболочка приложения, **закрытый технический тест в проде** | 10–14 рабочих дней |
| 3 | Платформенный слой Telegram: авторизация, Stars, приём забегов, основа ролей | 5–8 рабочих дней |
| 4 | Лидерборды, реклама, рефералка, шеринг, **каркас админ-панели** | 12–18 рабочих дней |
| 5 | Софт-лонч в Telegram; параллельно — рассылки, бухгалтерия, дашборды в панели | не меньше 14 календарных дней |
| 6 | Публичный запуск в Telegram, продвижение, конструктор аналитики и посты в каналы | 7 календарных дней активной фазы |
| 7 | Порт на MAX | 5–8 рабочих дней + до 2 календарных дней верификации |
| 8+ | VK и лайв-опс | — |

## Приоритет платформ: Telegram первым

**Решение (пересмотр первоначального плана)**: первой целевой платформой
становится Telegram, MAX — порт после публичного лонча в Telegram, VK — за
ним.

Первоначально в `00-README.md` и в этом файле MAX стоял первым (аргумент —
наименьшая конкуренция в молодой экосистеме). Аргумент остаётся верным, но
перевешивают три фактора:

1. **У команды уже есть продакшен-опыт именно в Telegram Mini Apps.**
   `vpnsibcom_api` + `vpnsibcom_web` — работающий продукт с авторизацией
   через `initData`, оплатой Stars, реферальной программой, рекламными
   сетями и аналитикой. Это не «похожий стек», а буквально код, который
   переносится (`13-reuse-from-vpnsibcom.md`). На MAX такого задела нет.
2. **Цикл тестирования короче.** Telegram-бот и Mini App поднимаются за
   минуты, тестируются на любом устройстве, без верификации бизнес-аккаунта
   и без ревью-лага площадки. MAX требует верифицированного бизнес-аккаунта
   на business.max.ru и модерации (`03-notes-and-risks.md`), что на этапе
   ежедневных итераций по геймплею — прямой тормоз.
3. **Схема валидации `initData` в MAX ещё не сверена с документацией** и
   помечена как открытый технический вопрос (`03-notes-and-risks.md`). Ставить
   первую платформу в зависимость от неразрешённого блокера — плохой размен;
   в Telegram эта часть уже решена и проверена в бою.

Что от этого меняется по существу:

- Раньше валидируем гипотезу по геймплею и рекламной монетизации — в
  Telegram доступны rewarded-форматы (Adsgram/Taddy/RichAds), недоступные в
  MAX (`01-tech-stack.md` §6). То есть монетизационная модель проверяется
  целиком, а не наполовину.
- Прайс-лист в первую очередь считается в Stars, рублёвый — при портировании
  на MAX. Это не конвертация курсом, экономика по платформам считается
  раздельно (`03-notes-and-risks.md`).
- Что **не** меняется: архитектура. `core-game` по-прежнему ничего не знает
  о платформе, `PlatformAdapter` остаётся единым контрактом
  (`01-tech-stack.md` §1). «Telegram первым» — это порядок реализации
  адаптеров, а не разрешение зашивать Telegram-специфику в движок. Заглушки
  `MaxAdapter`/`VkAdapter` остаются в репозитории с первого дня, чтобы
  платформенная граница не размылась.

## Обновление состава команды

Роадмап изначально писался под одного разработчика. Команда выросла до
трёх человек (техническая реализация, геймдизайн, технический
подбор/адаптация ассетов+монетизация — роли и границы в
`06-team-and-workflow.md`). Основной сдвиг:

- **Этап 1**: техническая часть (прототип, FPS-тест) — один человек
  (участник 1), остальные не заблокированы: геймдизайнер с первого дня
  прорабатывает список врагов/паттернов/кривую сложности как полноценный
  дизайн-документ, напарник параллельно стартует подбор и техническую
  адаптацию ассетов под этот дизайн
- **Этап 2**: собирается втроём — участник 1 реализует паттерны, апгрейды,
  оболочку приложения и инфраструктуру закрытого теста; геймдизайнер
  специфицирует врагов и апгрейды, наполняет таблицы и калибрует кривую;
  напарник задаёт визуальное направление дизайн-системы, интегрирует
  ассеты и ведёт ручное QA на устройствах. Распределение по пакетам работ —
  `26-stage2-plan.md` §3–§4
- **Этапы 3-4**: платформенный слой и бэкенд остаются зоной участника 1
  (специфика авторизации/оплаты не делится без дублирования контекста),
  напарник берёт на себя бизнес-сторону рекламы и монетизации
  (`07-monetization-and-ads.md`), полировка UI/UX и финализация экономики —
  совместно с геймдизайнером

## Этап 1 — Технический прототип и проверка гипотезы производительности

**Факт: 2–3 дня** (планировалась неделя)

**Цель: убедиться, что жанр вообще работоспособен внутри WebView, прежде чем вкладываться в контент**

- [x] Голый Phaser-прототип: перемещение персонажа, автоатака, спавн волн;
      симуляция отделена от рендера и работает headless
- [x] Собрать под Telegram (минимальная заглушка адаптера), заглушки MAX/VK не удалять
- [x] Стенд испытаний с нарастающей нагрузкой, метриками по таймлайну и
      автоотправкой отчётов (`25-week1-fps-trials.md`)
- [x] Прогон внутри реального клиента Telegram на трёх Android-аппаратах
      (TECNO CH7n, Realme RMX3627, Xiaomi 220333QNY) и Telegram Desktop
- [ ] **iOS не проверен** — устройства в команде нет; переносится в ручное QA
      этапа 2 (`26-stage2-plan.md`, WP11), до тех пор остаётся открытым риском
- [x] Решение "go/no-go" по движку: **GO**

**Критерий готовности**: стабильные 50+ FPS на бюджетном устройстве при 100
врагах на экране — **выполнен с запасом в 29-40 раз**: предел трёх бюджетных
Android — от 2872 до 4000+ врагов, тепловой деградации на рабочих нагрузках
нет (`25-week1-fps-trials.md` §6)

**Вопрос, переданный в этап 2**, — фиксировать ли частоту отрисовки на 60
для всех устройств (на 120-герцовом экране тот же телефон теряет 27% запаса
по нагрузке). **Решено: 60 FPS** (`16-tech-stack-decisions.md` §9.1)

## Этап 2 — Игровой цикл, оболочка приложения, закрытый тест

**Оценка: 10–14 рабочих дней.** Подробный план и принятые решения —
`26-stage2-plan.md`.

- [ ] 5–7 типов врагов с разным паттерном движения
- [ ] Постоянная атака; вид атаки задаётся стартовым оружием и улучшениями
      забега
- [ ] Опыт и уровни; выбор улучшения при наборе уровня — ориентир Vampire
      Survivors, адаптированный под нас
- [ ] Экран смерти со статистикой забега; главный показатель — время выживания
- [ ] Бесконечный мир с необязательными границами карты, спавн за видимой
      областью, динамическая камера
- [ ] Базовый баланс: кривые сложности и опыта
- [ ] Бесконечный режим на одной карте — единственный режим на старте
- [ ] Дизайн-система и первый UI приложения на React: адаптивный под любую
      ориентацию, с выбором полноэкранного или обычного режима; все разделы —
      визуальные заглушки (`27-design-system-and-app-shell.md`)
- [ ] Включаемый режим диагностики с запуском тестов производительности
      (`28-diagnostics.md` §2)
- [ ] Включаемая запись диагностики забега (`28-diagnostics.md` §3)
- [ ] Минимальный каркас телеметрии и выгрузка данных для разбора ИИ-агентом —
      перенесён сюда с этапа 3, без него закрытый тест не даёт статистики
- [ ] Быстрая выгрузка в Telegram: кнопка в боте, видна и работает только у
      администраторов, чьи Telegram ID заданы в окружении
      (`28-diagnostics.md` §6.1)
- [ ] Версия при мердже по типу изменения, объявленному в PR
      (`09-ci-cd.md` §8.1)
- [ ] Бэкенд сразу на целевом стеке, точные версии зависимостей везде
- [ ] Прод на существующем Caddy в Docker, отдельные поддомены, CDN Bunny.net
- [ ] Ручное QA на устройствах, включая iOS

Сознательно **вынесено** из прежнего плана недели 2: **локации (2–3
небольшие карты)** — карта одна, остальные сильно позже; сейчас важны
механики и работа приложения в целом.

**Критерий готовности**: версия из тега развёрнута в прод; бот закрытого
теста открывает приложение; тестер без программиста рядом играет полный забег
от старта до смерти в любой ориентации и любом режиме экрана, ходит по всем
разделам, при желании включает диагностику и запускает тест; события и
отчёты доходят до прод-БД и выгружаются для разбора. Полный чек-лист —
`26-stage2-plan.md` §6.

**Это не софт-лонч.** Закрытый тест этапа 2 — технический: без авторизации,
денег и рекламы, на небольшом круге тестеров. Софт-лонч этапа 5 — продукт
целиком на реальной аудитории.

## Этап 3 — Платформенный слой: Telegram

**Оценка: 5–8 рабочих дней**

- [ ] `TelegramAdapter`: авторизация и оплата. Интерфейс `PlatformAdapter` и
      UI-возможности адаптера (fullscreen, кнопка «назад», безопасная зона)
      появляются уже на этапе 2
- [ ] Авторизация через Telegram WebApp SDK, валидация `initData` на бэке
      (`@tma.js/init-data-node`, перенос из `vpnsibcom_api` —
      `13-reuse-from-vpnsibcom.md` §2). Проверка подписи для приёмников
      телеметрии уже есть с этапа 2 — переиспользуется
- [ ] JWT access/refresh + ротация и блэклист в Redis (перенос, там же §2)
- [ ] Интеграция оплаты через **Telegram Stars** — минимум 2-3 SKU (континью, скин, снятие ограничения);
      континью подключается к блоку «Второй шанс», который уже стоит
      заглушкой на экране смерти (`07-monetization-and-ads.md` §8)
- [ ] Модуль `runs` на бэке — приём результатов забега с базовой антифрод-проверкой
- [ ] `startParam` → источник трафика/реферал: парсинг и запись сессии
      (перенос `parse-start-param.util` + `Sessions`/`Acquisition`, там же §4)
- [ ] Аналитика: события аккаунта и сессий поверх каркаса этапа 2;
      привязка истории закрытого теста к аккаунтам по `platform_user_id`
      (`22-analytics-and-metrics.md` §8)
- [ ] Основа админ-панели на бэкенде: модель прав и ролей, назначение ролей,
      аудит действий — вместе с авторизацией, чтобы права не пришлось
      вшивать в готовые модули задним числом (`29-admin-panel.md` §3)

**Критерий готовности**: можно зайти в мини-апп в Telegram под своим аккаунтом, сыграть, купить континью за Stars

## Этап 4 — Лидерборды, реклама, полировка, экономика

**Оценка: 12–18 рабочих дней** — самый широкий этап, с каркасом админ-панели
вырос ещё; на старте стоит пересмотреть, не разделить ли его на два

- [ ] Лидерборд: глобальный + "среди контактов" (см. заметки о вирусности), Redis ZSET
- [ ] Шеринг результата забега в чат одним тапом
- [ ] Rewarded-реклама за континью: цепочка провайдеров с fallback
      (Adsgram/Taddy/RichAds — перенос модуля `ads`, `13-reuse-from-vpnsibcom.md` §3)
- [ ] Реферальная программа: ссылки, привязка, активация по достигнутой
      волне, награды, базовый антифрод (`23-referral-and-partner-program.md` §2)
- [ ] Редирект-страница `/r/:code`: сбор UTM и антифрод-данных, запись
      клика, deep link в Telegram (`24-attribution-and-sharing.md` §3)
- [ ] Шеринг результата забега с вшитой реферальной ссылкой, картинка
      генерируется на сервере и кешируется (там же §7)
- [ ] События забегов, покупок, рекламы, рефералов; живые счётчики в Redis
      (`22-analytics-and-metrics.md` §8)
- [ ] Заглушки разделов этапа 2 становятся рабочими — в порядке интереса
      тестеров (`27-design-system-and-app-shell.md` §6): награда дня,
      задания и достижения, колесо удачи — с выдачей наград сервером
      (`05-game-design.md` §3, `07-monetization-and-ads.md` §7)
- [ ] «Второй шанс» после смерти за rewarded-рекламу; движок откладывает
      закрытие забега до отказа от продолжения (`07-monetization-and-ads.md` §8)
- [ ] Полировка UI/UX (первое впечатление критично для retention)
- [ ] Финализация прайс-листа SKU в Stars
- [ ] **Админ-панель, каркас** (`29-admin-panel.md`): отдельное приложение,
      вход через Telegram, роли по матрице прав, аудит в интерфейсе. Модули:
  - **контент и баланс, фаза 1** (`19-content-admin.md` §9): конфигурация
    переезжает в БД и публикуется версионированным снапшотом, редактор с
    валидацией, прогоном golden-сценариев, публикацией и откатом. Без этого
    пункт этапа 5 про быстрые итерации баланса невыполним: каждая правка
    требовала бы нового билда клиента и модерации площадки;
  - игроки: карточка, блокировка, очередь антифрода;
  - реклама, фича-флаги, отчёты диагностики;
  - готовые дашборды **Live** и **Геймплей и баланс**
- [ ] Техническая Grafana: задержки, ошибки, очереди, алерты — только
      техника, продуктовые дашборды живут в панели

**Критерий готовности**: готов конечный продукт для реальных пользователей, не только для тебя

## Этап 5 — Софт-лонч в Telegram

**Не меньше 14 календарных дней**: D7 retention первой когорты виден не
раньше восьмого дня, и нужно время хотя бы на одну итерацию по нему

- [ ] Закрытый запуск на небольшую тестовую аудиторию (свой канал/знакомые)
- [ ] Мониторинг: retention D1/D3/D7, крэши, конверсия в покупку, eCPM rewarded-показов
- [ ] Быстрые итерации по балансу на основе реальных данных — через админку,
      без пересборки клиента (`19-content-admin.md`)
- [ ] Первые пользовательские отзывы → приоритизация правок
- [ ] Витрины, retention; в админ-панели — дашборды Монетизация,
      Привлечение, Реклама, Рефералы — **до** запуска посевов, иначе оценивать
      их будет нечем (`04-marketing-plan.md`)
- [ ] Решение о режиме диагностики для публичных игроков (`28-diagnostics.md` §2.4)

Разработка идёт **параллельно со сбором данных**, пока когорты набирают дни:

- [ ] **Рассылки пользователям в бота** (`29-admin-panel.md` §7): сегмент
      аудитории по фильтрам — платформа, язык, источник и кампания, партнёр,
      давность и активность, прогресс, место в топе, платящий или нет; тип
      сообщения — сервисное, информационное, удерживающее, рекламное;
      предпросмотр и тестовая отправка, расписание, лимиты частоты на игрока,
      отслеживаемые ссылки, аналитика доставки и переходов, правило двух
      ключей для больших и рекламных рассылок
- [ ] Момент в игре, где игрок разрешает боту писать ему, — без разрешения
      рассылка до него не доходит
- [ ] **Бухгалтерия, фаза 1** (`29-admin-panel.md` §6): ручной ввод доходов,
      расходов и займов с шаблонами, закрытие месяца, расчёт распределения
      дохода по `11-revenue-split.md`, отчёт участникам в панели

**Критерий готовности**: retention и конверсия не хуже ожидаемых по бенчмаркам жанра, нет критичных багов

## Этап 6 — Публичный запуск в Telegram + начало пиара

**7 календарных дней активной фазы**, дальше — лайв-опс

- [ ] Публичный релиз, версия `1.0.0` (`09-ci-cd.md` §8.1)
- [ ] Запуск плана продвижения (см. `04-marketing-plan.md`)
- [ ] Живое отслеживание метрик, готовность к быстрым патчам
- [ ] **Постинг в каналы** из админ-панели: черновики, расписание,
      закрепление, шаблон поста о патче из заметок релиза, отслеживаемые
      ссылки, маркировка рекламы (`29-admin-panel.md` §7.4)
- [ ] **Конструктор графиков** и сохранённые дашборды в админ-панели
      (`29-admin-panel.md` §5.2)

## Этап 7 — Порт на MAX

**Оценка: 5–8 рабочих дней** плюс до двух календарных дней на верификацию

- [ ] Снять блокер: сверить схему валидации initData MAX Bridge с актуальной
      документацией dev.max.ru (`03-notes-and-risks.md`) — **начать заранее, на
      этапе 5**, не в момент старта портирования
- [ ] Верификация бизнес-аккаунта на business.max.ru (1-2 дня буфера, там же)
- [ ] `MaxAdapter`: авторизация через MAX Bridge, оплата через СБП/VK Pay
- [ ] Отступы безопасной зоны и прочие UI-возможности адаптера под MAX
      (`27-design-system-and-app-shell.md` §5.1)
- [ ] Пересчёт прайс-листа в рубли (не конвертация из Stars курсом — отдельная экономика)
- [ ] Отключение rewarded-кнопок там, где формат недоступен (`01-tech-stack.md` §6)
- [ ] Кросс-промо между площадками (не дублировать лидерборды — либо разделять, либо объединять сознательно)

## Этап 8+ — VK и лайв-опс

- VK Mini Apps: `VkAdapter`, авторизация по `sign`, нативная rewarded-реклама
  (единственная площадка, где она есть «из коробки», `01-tech-stack.md` §6)
- **Партнёрская программа** (`23-referral-and-partner-program.md` §4):
  сущность партнёра, кабинет с дашбордами на слое аналитики админ-панели,
  промокоды, начисления и выплаты по RevShare/FTD (`29-admin-panel.md` §5.4).
  Сознательно после лонча — до появления стабильной конверсии в платящих
  партнёру нечего предлагать и не из чего платить
- Бухгалтерия, фаза 2: импорт выгрузок рекламных сетей и платёжных
  провайдеров, затем автоимпорт там, где окупается
- Новые карты — вместе с остальными контент-дропами (ниже)
- Хаб-страница выбора платформы, карточка профиля с вариантами оформления,
  шеринг в истории, A/B текстов (`24-attribution-and-sharing.md` §9)
- Жанр требует регулярных контент-дропов для удержания аудитории (новые враги,
  локации, сезонный контент) — без этого retention после первого месяца обычно
  проседает. Это отдельный процесс после лонча, не часть MVP-скоупа
- **Пропуск: ежемесячный и сезонный** — бесплатная и платная линейки, прогресс
  за задания, фиксированная цена (`07-monetization-and-ads.md` §9). Сезонный
  выходит вместе с контент-дропом сезона
