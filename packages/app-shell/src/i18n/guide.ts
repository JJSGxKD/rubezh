import { addTranslations } from "./index";
import guide from "./ru-guide.json";

/**
 * Тексты гайдбука. Модуль импортирует сам экран гайдбука: словарь дополняется
 * в момент загрузки его чанка, до первой отрисовки.
 *
 * Отдельно от основного словаря по той же причине, что и тексты команды:
 * девяносто ключей про поведение врагов и оружия нужны на одном экране, а
 * весят как треть всего словаря — в первую загрузку они не едут.
 */
addTranslations(guide);

export const GUIDE_TRANSLATIONS_READY = true;
