import type { ContinueDef } from "@bh/shared-types";

// Второй шанс — продолжение забега после смерти за Stars или, позже, за
// рекламу (docs/07-monetization-and-ads.md §8). Продолжение убирает с поля
// всех врагов и их снаряды — без добычи, иначе второй шанс был бы выгоднее,
// чем не умирать, — возвращает здоровье и даёт короткую неуязвимость.
//
// Числа рабочие, до решения геймдизайнера (docs/34-stage3-plan.md, О1):
// одно продолжение за забег, здоровье целиком и три секунды неуязвимости —
// столько нужно, чтобы понять, откуда идёт новая волна.
export const CONTINUE: ContinueDef = {
  perRun: 1,
  restoreHpRatio: 1,
  invulnerableSec: 3,
};
