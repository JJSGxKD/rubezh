# Версия Node проверяется по полю engines корневого package.json —
# одна версия у всех и в CI, см. docs/16-tech-stack-decisions.md §1.
engine-strict=true
# Новые зависимости сразу записываются точной версией, без ^ и ~
# (docs/16-tech-stack-decisions.md §9).
save-exact=true
